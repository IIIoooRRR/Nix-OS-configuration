{ config, pkgs, ... }:
  users.users.kezess = {
    isNormalUser = true;
    description = "Kezess";
    extraGroups = [ "networkmanager" "wheel" ];
    packages = with pkgs; [
      kdePackages.kate
    ];
  };
